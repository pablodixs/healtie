import { css } from '../../../styled-system/css'

export const mainContainer = css({
    display: 'flex',
    flexDirection: 'column',
    flex: 1,
    alignItems: 'center',
    minHeight: '95dvh',
})

export const headingHiddenContainer = css({
    display: 'none',
    alignItems: 'center',
    gap: '1ch',
})

export const searchBarContainer = css({ position: 'relative' })
